# ──────────────────────────────────────────────
# Firebase Configuration
# Lấy từ: Firebase Console → Project Settings → General → Your apps → Web app
# https://console.firebase.google.com/project/_/settings/general
# ──────────────────────────────────────────────

# API Key — Firebase Console → Project Settings → General → Web API Key
VITE_FIREBASE_API_KEY=your_firebase_api_key

# Auth Domain — format: your-project-id.firebaseapp.com
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Project ID — Firebase Console → Project Settings → General → Project ID
VITE_FIREBASE_PROJECT_ID=your-project-id

# Storage Bucket — format: your-project-id.firebasestorage.app
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app

# Messaging Sender ID — Firebase Console → Project Settings → Cloud Messaging → Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id

# App ID — Firebase Console → Project Settings → General → Your apps → App ID
VITE_FIREBASE_APP_ID=your_app_id

# (Optional) Measurement ID — chỉ cần nếu enable Google Analytics
# VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ──────────────────────────────────────────────
# Gemini API
# Lấy từ: https://aistudio.google.com/apikey
# ──────────────────────────────────────────────
VITE_GEMINI_API_KEY=your_gemini_api_key
